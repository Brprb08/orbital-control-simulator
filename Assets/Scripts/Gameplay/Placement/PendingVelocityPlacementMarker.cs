using UnityEngine;

/// <summary>
/// Marks the real manual-placement satellite while it is waiting for velocity input.
/// Camera code uses this to distinguish it from the pre-placement ghost preview.
/// </summary>
public sealed class PendingVelocityPlacementMarker : MonoBehaviour
{
}
